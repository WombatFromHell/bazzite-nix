name: Push and Sign
description: Push container image to registry, sign with Cosign, and verify

inputs:
  source_ref:
    description: "Container storage reference for the built image"
    required: true
  variant_name:
    description: "Variant name"
    required: true
  tags:
    description: "Comma-separated list of tags"
    required: true
  signing_secret:
    description: "Cosign signing secret"
    required: true
  github_token:
    description: "GitHub token for registry auth"
    required: true
  github_actor:
    description: "GitHub actor username"
    required: true
  registry:
    description: "Registry URL (e.g., ghcr.io/owner)"
    required: true
  repo:
    description: "Repository name"
    required: true
  suffix:
    description: "Variant suffix (e.g., -nvidia-open)"
    required: false
    default: ""
  date:
    description: "Build timestamp"
    required: true
  parent_version:
    description: "Parent image version"
    required: true
  max_attempts:
    description: "Maximum retry attempts for push/sign operations"
    required: false
    default: "3"
  retry_delay:
    description: "Delay in seconds between retry attempts"
    required: false
    default: "15"

outputs:
  remote_digest:
    description: "Remote image digest"
    value: ${{ steps.push_image.outputs.remote_digest }}
  remote_digest_ref:
    description: "Full remote image reference with digest"
    value: ${{ steps.push_image.outputs.remote_digest_ref }}
  verification_status:
    description: "Verification status (success/failure)"
    value: ${{ steps.sign_and_verify.outputs.status }}

runs:
  using: composite
  steps:
    - name: Login to GHCR
      id: login
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
        GITHUB_ACTOR: ${{ inputs.github_actor }}
        MAX_ATTEMPTS: ${{ inputs.max_attempts }}
        RETRY_DELAY: ${{ inputs.retry_delay }}
      run: |
        set -euo pipefail
        # shellcheck source=helpers.sh
        source "${GITHUB_ACTION_PATH}/helpers.sh"

        mkdir -p /tmp/skopeo-auth
        run_with_retry "skopeo login ghcr.io" \
          --stdin-data "${GITHUB_TOKEN}" \
          skopeo login ghcr.io \
            --username "${GITHUB_ACTOR}" \
            --password-stdin \
            --authfile /tmp/skopeo-auth/auth.json

    - name: Push to GHCR
      id: push_image
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
        GITHUB_ACTOR: ${{ inputs.github_actor }}
        SOURCE_REF: ${{ inputs.source_ref }}
        TAGS: ${{ inputs.tags }}
        MAX_ATTEMPTS: ${{ inputs.max_attempts }}
        RETRY_DELAY: ${{ inputs.retry_delay }}
        BASE_IMG: ${{ inputs.registry }}/${{ inputs.repo }}${{ inputs.suffix }}
      run: |
        set -euo pipefail
        # shellcheck source=helpers.sh
        source "${GITHUB_ACTION_PATH}/helpers.sh"

        IFS=',' read -r -a TAGS_ARR <<< "${TAGS}"

        echo "::group::Push primary tag (${TAGS_ARR[0]})"
        echo "Source ref : ${SOURCE_REF}"
        echo "Target     : docker://${BASE_IMG}:${TAGS_ARR[0]}"
        skopeo_copy_with_retry \
          "${SOURCE_REF}" \
          "docker://${BASE_IMG}:${TAGS_ARR[0]}"
        echo "::endgroup::"

        echo "::group::Inspect + verify primary digest"
        REMOTE_DIGEST=$(sudo skopeo inspect \
          --authfile /tmp/skopeo-auth/auth.json \
          --format='{{.Digest}}' \
          "docker://${BASE_IMG}:${TAGS_ARR[0]}")

        [[ -z "$REMOTE_DIGEST" ]] && \
          { echo "::error::inspect returned empty digest"; exit 1; }

        verify_digest "${BASE_IMG}:${TAGS_ARR[0]}" "$REMOTE_DIGEST"

        SHORT_DIGEST="${REMOTE_DIGEST#sha256:}"
        REMOTE_DIGEST_REF="${BASE_IMG}@${REMOTE_DIGEST}"
        echo "Digest     : ${REMOTE_DIGEST}"
        echo "Digest ref : ${REMOTE_DIGEST_REF}"
        echo "::endgroup::"

        echo "::group::Push additional tags"
        for tag in "${TAGS_ARR[@]:1}" "$SHORT_DIGEST"; do
          echo "  → ${BASE_IMG}:${tag}"
          skopeo_copy_with_retry \
            "docker://${REMOTE_DIGEST_REF}" \
            "docker://${BASE_IMG}:${tag}"
        done
        echo "::endgroup::"

        echo "remote_digest=${REMOTE_DIGEST}"         >> "$GITHUB_OUTPUT"
        echo "remote_digest_ref=${REMOTE_DIGEST_REF}" >> "$GITHUB_OUTPUT"

    - name: Install Cosign
      uses: sigstore/cosign-installer@ba7bc0a3fef59531c69a25acd34668d6d3fe6f22 # v4.1.0

    - name: Sign and verify image
      id: sign_and_verify
      shell: bash
      env:
        SIGNING_SECRET: ${{ inputs.signing_secret }}
        GITHUB_TOKEN: ${{ inputs.github_token }}
        GITHUB_ACTOR: ${{ inputs.github_actor }}
        DIGEST_REF: ${{ steps.push_image.outputs.remote_digest_ref }}
        VARIANT_NAME: ${{ inputs.variant_name }}
        TAGS: ${{ inputs.tags }}
        DATE: ${{ inputs.date }}
        PARENT_VERSION: ${{ inputs.parent_version }}
        REGISTRY: ${{ inputs.registry }}
        REPO: ${{ inputs.repo }}
        SUFFIX: ${{ inputs.suffix }}
        MAX_ATTEMPTS: ${{ inputs.max_attempts }}
        RETRY_DELAY: ${{ inputs.retry_delay }}
      run: |
        set -euo pipefail
        # shellcheck source=helpers.sh
        source "${GITHUB_ACTION_PATH}/helpers.sh"

        echo "::group::Sign image"
        run_with_retry "cosign sign ${DIGEST_REF}" \
          cosign sign -y \
            --key env://SIGNING_SECRET \
            --new-bundle-format=false \
            --use-signing-config=false \
            --registry-referrers-mode=legacy \
            --registry-username "${GITHUB_ACTOR}" \
            --registry-password "${GITHUB_TOKEN}" \
            "${DIGEST_REF}"
        echo "::endgroup::"

        echo "::group::Verify image"
        run_with_retry "cosign verify ${DIGEST_REF}" \
          cosign verify \
            --key ./cosign.pub \
            "${DIGEST_REF}"
        echo "::endgroup::"

        echo "::group::Inspect layer sizes"
        TOTAL_MB=$(sudo skopeo inspect --raw \
          --authfile /tmp/skopeo-auth/auth.json \
          "docker://${DIGEST_REF}" | \
          jq '([.layers[].size] | add) / 1024 / 1024 | round')
        echo "Total compressed size: ${TOTAL_MB} MB"
        echo "::endgroup::"

        REMOTE_DIGEST="${DIGEST_REF#*@sha256:}"
        TAGS_FMT=$(echo "${TAGS}" | tr ',' ' · ')

        {
          echo "## ${VARIANT_NAME}"
          echo ""
          echo "| Field | Value |"
          echo "| ----- | ----- |"
          echo "| **Image**             | \`${REGISTRY}/${REPO}${SUFFIX}\` |"
          echo "| **Digest**            | \`${REMOTE_DIGEST}\` |"
          echo "| **Tags**              | \`${TAGS_FMT}\` |"
          echo "| **Parent version**    | \`${PARENT_VERSION}\` |"
          echo "| **Built**             | \`${DATE}\` |"
          echo "| **Image size**        | \`${TOTAL_MB} MB\` |"
          echo "| **Signed & verified** | ✅ |"
        } >> "$GITHUB_STEP_SUMMARY"

        echo "status=success" >> "$GITHUB_OUTPUT"
