name: Clean Old Images

on:
  schedule:
    - cron: "42 22 2,16 * *" # Runs at 22:42 UTC on the 2nd and 16th of every month
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry-run mode (safe: no deletions)"
        type: boolean
        default: true
      package_filter:
        description: "Comma-separated list of full package names to process (empty = all cleanup-enabled)"
        type: string
        default: ""

env:
  # Scheduled runs default to live mode; manual runs respect dry_run input
  DRY_RUN: ${{ github.event_name == 'schedule' && 'false' || (inputs.dry_run == true && 'true' || 'false') }}

jobs:
  prepare:
    runs-on: ubuntu-26.04
    outputs:
      packages: ${{ steps.variants.outputs.packages }}
      owner_type: ${{ steps.detect_owner.outputs.owner_type }}
      registry_owner: ${{ steps.variants.outputs.registry_owner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Detect owner type for API calls
        id: detect_owner
        run: |
          if gh api "/orgs/${{ github.repository_owner }}" &>/dev/null; then
            OWNER_TYPE="orgs"
          else
            OWNER_TYPE="users"
          fi
          echo "owner_type=${OWNER_TYPE}" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build package matrix
        id: variants
        run: |
          FILTER="${{ inputs.package_filter }}"
          BASE="bazzite-nix"
          if [ -n "$FILTER" ]; then
            jq -r --arg base "$BASE" \
              '[.variants[] | ($base + (.suffix // "")) | ascii_downcase] | unique[]' \
              .github/variants.json > /tmp/all_packages.txt
            grep -Fxf <(echo "$FILTER" | tr ',' '\n' | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//') \
              /tmp/all_packages.txt > /tmp/packages.txt 2>/dev/null || touch /tmp/packages.txt
            [ -s /tmp/packages.txt ] || echo "::warning::package-filter '${FILTER}' matched no packages"
          else
            jq -r --arg base "$BASE" \
              '[.variants[] | select(.disabled != true) | select(.cleanup != false) | ($base + (.suffix // "")) | ascii_downcase] | unique[]' \
              .github/variants.json > /tmp/packages.txt
          fi

          echo "Packages to process:"
          cat /tmp/packages.txt

          REPO_LC="${GITHUB_REPOSITORY#*/}"
          {
            echo "packages=$(jq -Rnc '[inputs]' < /tmp/packages.txt)"
            echo "registry_owner=${REPO_LC,,}"
          } >> "$GITHUB_OUTPUT"

  cleanup-images:
    needs: [prepare]
    runs-on: ubuntu-26.04
    strategy:
      fail-fast: false
      matrix:
        package: ${{ fromJson(needs.prepare.outputs.packages) }}

    # Prevent concurrent cleanup of same package
    concurrency:
      group: cleanup-image-${{ matrix.package }}
      cancel-in-progress: false

    permissions:
      packages: write

    steps:
      - name: Set registry env
        run: |
          {
            echo "REGISTRY_OWNER_LC=${{ needs.prepare.outputs.registry_owner }}"
            echo "OWNER_TYPE=${{ needs.prepare.outputs.owner_type }}"
          } >> "$GITHUB_ENV"

      - name: Cleanup container images
        uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          package: ${{ matrix.package }}
          older-than: 60 days
          keep-n-tagged: 24
          delete-untagged: true
          delete-partial-images: true
          delete-orphaned-images: true
          dry-run: ${{ env.DRY_RUN }}
          log-level: info
